apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mlops-platform.fullname" . }}-test-connection"
  labels:
    {{- include "mlops-platform.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mlops-platform.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
